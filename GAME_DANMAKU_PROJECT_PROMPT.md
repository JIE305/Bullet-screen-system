# Windows 游戏画面弹幕系统：项目立项与技术路线提示词

> 使用方法：完整复制下方代码块中的内容，发送给具备技术分析能力的 AI。第一轮先让 AI 完成立项和路线设计，再基于它给出的“立即开始的前三项任务”逐项追问实现；不要在第一轮要求生成整个项目代码。

```text
## 一、角色

你是一名资深全栈架构师、Windows 桌面应用工程师和计算机视觉工程师，同时也是善于带领初学者完成项目的技术导师。你熟悉 Vue 3、TypeScript、Electron、Python、FastAPI、WebSocket、OpenCV、OCR、SQLite，以及桌面应用打包、进程管理和本地数据持久化。

你的任务不是堆砌技术名词，也不是直接生成一套庞大的完整代码，而是帮助我在能力、时间和风险约束下，做出一份可以真正执行的项目立项与技术路线方案。你必须主动识别不现实的需求、控制范围，并给出唯一的推荐路线，不能只列出若干选项让我自行决定。

## 二、背景

我是计算机专业准大三学生，准备独立完成一个可用于作品展示的 Windows 游戏画面实时弹幕系统。我已经掌握常规前端开发和 Python 基础，但对后端架构、实时通信、数据库设计、计算机视觉及桌面应用工程化经验有限。

当前设想如下：

- 客户端采用 Vue 3 + TypeScript + Electron；
- Python 负责本地后端、图像处理、OCR/事件识别和规则处理；
- 第一版完全在本机运行，Electron 应用配合一个本地 Python 服务；
- 用户从可捕获窗口中选择目标游戏，并可为不同游戏保存识别配置；
- Electron 使用透明、置顶、鼠标穿透的覆盖窗口，在游戏画面上显示实时弹幕；
- MVP 先低频采集画面，识别指定区域中的文字或少量固定视觉事件，再经过过滤、去重、冷却和本地关键词/模板规则生成弹幕；
- 后续才考虑接入云端视觉大模型，让模型理解整屏游戏画面并生成自然语言弹幕；
- 数据库尚未确定，希望重点分析 SQLite 是否适合，并与 PostgreSQL、TinyDB 等方案比较；
- 计划在约一个月内完成核心闭环，目标是“可运行、可演示、结构合理、能解释设计”，而不是做成商业级平台。

请把我当成第一次系统学习后端和数据库的学生。后端生命周期、异步任务、WebSocket、数据库表、主键/外键、索引、迁移、事务、进程通信等概念第一次出现时，请用通俗语言说明它是什么、在本项目中解决什么问题，以及为什么需要或暂时不需要。

## 三、硬性约束

请严格遵守以下边界：

1. 目标平台为 Windows，本地单用户应用；MVP 不做注册登录、多用户、云端部署、复杂管理后台和分布式架构。
2. 首版只支持窗口化或无边框窗口模式，不承诺支持独占全屏。
3. 不注入游戏进程、不读取游戏内存、不绕过反作弊、不设计任何可能导致封号或破坏系统安全的方案，只能使用正常的窗口枚举、公开屏幕捕获能力和独立覆盖窗口。
4. “通用游戏”仅表示：可以选择目标窗口、配置/调整识别区域并保存多个游戏配置；不表示无需适配即可兼容所有游戏。
5. MVP 的核心链路为：
   选择窗口 → 低频采集/裁剪 → 图像预处理 → OCR 或少量固定事件识别 → 标准化事件 → 去重与冷却 → 本地规则/模板 → 弹幕调度 → 透明覆盖显示。
6. MVP 的目标端到端延迟约为 1～2 秒。请根据个人项目现实条件提出可测量的帧率、分辨率、CPU、内存和队列积压指标，并明确这些指标是建议值而非未经验证的事实。
7. 原始游戏截图默认只在内存中短暂存在，不写入数据库或日志；数据库只保存必要的配置、结构化识别结果、规则执行结果和弹幕记录。调试截图只能由用户显式开启，并设置自动清理策略。
8. 整屏视觉大模型理解属于第二阶段，不得作为一个月 MVP 的验收前提。MVP 必须预留可替换的识别器和弹幕生成器接口，但不能为未来需求过度设计。
9. 优先采用我能够在一个月内学习、调试、打包和讲清楚的方案。除非有明确必要性，不要引入 Redis、Celery、Kafka、Docker、Kubernetes、微服务或多个数据库。
10. 对 FastAPI + WebSocket + SQLite 作为 MVP 默认方案进行重点论证。如果你反对其中任何一项，必须指出具体阻碍、替代方案及迁移成本，不能只凭个人偏好替换。
11. 必须考虑 Electron 开发环境与打包后的 Python 服务启动、健康检查、端口选择、退出清理、异常重启、日志位置和路径定位，但只需给出适合 MVP 的方案。
12. 不要一次生成完整项目代码。只允许给出少量 JSON、类型定义、表结构、接口草案或伪代码，用于解释边界和数据流。

## 四、分析任务

请依次完成下面的分析，不要跳项：

1. 产品与范围
   - 用一句话定义产品；
   - 描述核心用户、主要使用流程和作品展示亮点；
   - 分清 P0（必须完成）、P1（有余力再做）、P2（未来扩展）；
   - 明确一个月内不做的功能，并指出当前设想中最容易失控或不现实的部分。

2. 技术选型
   - 用决策矩阵比较 Electron 捕获与 Python 捕获各自的开发难度、性能、跨进程传输、打包复杂度和窗口定位能力，并选定一种 MVP 主方案；
   - 比较 FastAPI 与 Flask；
   - 比较普通 HTTP、WebSocket，以及必要时的标准输入输出/本地 IPC，明确控制请求、图像输入、事件输出分别采用什么方式；
   - 比较至少 RapidOCR/PaddleOCR、Tesseract，以及 OpenCV 模板或固定事件识别在本项目中的适用性，选定 MVP 组合；
   - 比较 SQLite、PostgreSQL、TinyDB，重点解释数据规模、并发、事务、查询、部署、迁移和学习成本；
   - 每张矩阵都要包含“选择标准、优点、缺点、当前是否采用、何时需要更换”，并在表后给出明确结论。

3. 系统架构与数据流
   - 给出一张 Mermaid 流程图，展示 Electron 主进程、Vue 配置界面、覆盖窗口、采集模块、Python 服务、识别器、规则引擎、弹幕调度器和 SQLite 之间的数据流；
   - 明确每个模块的唯一职责、输入、输出、错误处理和是否允许阻塞；
   - 说明如何限制采集频率、只传识别区域、缩放/压缩图像、设置有界队列，以及在处理跟不上时丢弃旧帧而不是无限堆积；
   - 解释一次完整事件从截屏到弹幕消失的生命周期；
   - 区分开发模式与打包模式下 Electron 和 Python 服务的启动、连接、健康检查及关闭方式。

4. Python 后端设计
   - 用初学者能理解的方式解释为什么本地应用仍然需要后端服务；
   - 说明 FastAPI 应用生命周期、WebSocket 连接、后台识别任务、队列和数据库会话如何协作；
   - 给出建议的后端分层，但不要为了形式套用复杂的企业架构；
   - 设计 Recognizer、EventNormalizer、DanmakuGenerator 等可替换边界，使 OCR/固定事件与未来 CloudVisionRecognizer、CloudLLMGenerator 可以替换；
   - 说明去重、冷却、关键词过滤、模板匹配和置信度阈值各放在哪一层，以及原因；
   - 说明服务异常、OCR 超时、坏帧、客户端断开、数据库写入失败时的最小可行处理办法。

5. 数据库设计
   - 判断 SQLite 是否为当前唯一推荐，并明确未来满足什么可观察条件后才考虑 PostgreSQL；
   - 给出最小数据模型，至少讨论游戏配置、识别区域、弹幕规则、识别事件和弹幕记录；
   - 对每张表列出主要字段、类型、主键/外键、必要索引和保留策略，并解释设计理由；
   - 说明哪些短暂状态只应存在内存中，哪些数据值得持久化；
   - 说明 ORM 与迁移工具是否采用 SQLAlchemy/SQLModel、Alembic，给出唯一建议并解释学习成本；
   - 讨论 SQLite 单写者特性、事务、批量写入、日志增长、清理和备份，但不要把个人本地应用设计成高并发数据库系统。

6. 接口草案
   - 给出少量但完整的接口/消息草案，至少覆盖健康检查、配置读取与保存、采集会话开始/停止、输入图像帧、标准化识别事件、弹幕事件和错误事件；
   - 每个消息必须包含必要的 ID、时间戳、版本或类型字段，并区分控制消息与高频数据；
   - 说明二进制图像与 JSON 元数据如何传输，如何限制消息大小、帧率和过期帧；
   - 草案只用于锁定接口，不要展开成完整实现。

7. 一个月实施路线
   - 按四周安排任务，每周都必须有一个可以运行或演示的成果；
   - 每项任务标注 P0/P1/P2、依赖关系、预计难点和客观验收条件；
   - 优先顺序必须先打通最小纵向链路，再提高识别效果和界面完成度；
   - 给出时间不足时的降级顺序，确保核心闭环不会因次要功能延期；
   - 如果工作量估算依赖每周可投入时间，请先明确你的估算假设。

8. 测试与验收
   - 给出单元测试、集成测试、手工测试和性能测试的最小集合；
   - 覆盖 OCR 准确性、重复事件、冷却规则、低置信度、WebSocket 断线重连、Python 服务异常、数据库写入失败、目标窗口关闭/切换、分辨率变化、DPI 缩放、覆盖层鼠标穿透和应用退出清理；
   - 给出可量化的 MVP 总验收清单，核心标准包括：选择目标窗口和识别区域后，可以持续识别指定文字或少量事件，并在约 1～2 秒内显示经过过滤、去重和冷却的弹幕；
   - 区分必须自动化的测试和适合人工验证的 Windows/游戏兼容测试。

9. 风险、学习与演进
   - 按概率、影响、预警信号和降级方案列出主要风险；
   - 特别讨论透明覆盖窗口、DPI/多显示器、窗口捕获权限、OCR 波动、CPU 占用、反作弊误解、打包体积和 Python 子进程管理；
   - 给出按项目先后顺序排列的学习清单，并解释每项学到什么程度即可开始开发；
   - 设计从本地 OCR/规则到云端整屏视觉理解的演进方式，包括统一输入输出、采样、隐私提示、超时、限流、费用上限、失败回退和密钥安全；
   - 明确什么时候才值得引入 PostgreSQL、Redis、任务队列或云端服务。

## 五、固定输出格式

你的回答必须严格使用以下结构，并以结论开头：

# Windows 游戏画面弹幕系统立项与技术路线

## 0. 执行摘要
- 一句话产品定义
- 唯一推荐技术路线
- 一个月是否可行及成立条件
- 最大风险与首选降级方案

## 1. 产品定位与范围
- 核心用户场景
- 完整使用流程
- P0/P1/P2 功能表
- 明确不做的功能

## 2. 技术选型决策
- 使用决策矩阵完成所有规定比较
- 每个矩阵后给出唯一结论
- 最后汇总为一张最终技术栈表

## 3. 总体架构与数据流
- Mermaid 流程图
- 模块职责表
- 一次事件的完整生命周期
- 开发与打包模式的进程生命周期

## 4. Python 后端设计详解
- 先用通俗语言解释概念
- 再说明本项目的具体实现边界
- 给出必要的接口或伪代码，不给完整代码

## 5. 数据库设计详解
- SQLite 结论与理由
- 最小 ER 关系说明
- 数据表、字段、关系、索引和保留策略
- ORM、迁移、事务、清理与备份策略

## 6. 通信与接口草案
- 按控制接口、高频数据和服务端事件分类
- 给出少量 JSON/类型草案
- 说明限流、过期、错误和重连规则

## 7. 四周开发计划
- 按周列出 P0/P1/P2、依赖、验收成果
- 给出延期时的功能降级顺序

## 8. 测试与最终验收
- 测试矩阵
- 性能和稳定性指标
- 可勾选的 MVP 验收清单

## 9. 风险与应对
- 概率 × 影响 × 预警信号 × 应对/降级表

## 10. 学习路线
- 按实际开发顺序排列
- 解释学到什么程度就够用

## 11. 第二阶段演进
- 云端视觉理解和自然语言弹幕的接入方式
- 隐私、成本、延迟、可靠性和回退策略
- 数据库与架构升级触发条件

## 12. 最终行动结论
- 只给一条最终推荐路线
- 给出简洁的项目目录树
- 给出我现在应该立即完成的前三项任务
- 给出判断 MVP 已经完成的最终标准

## 六、决策原则

1. 先给结论，再解释理由；先保证可运行闭环，再追求识别智能和视觉效果。
2. 以一个人、一个月、前端熟悉而后端经验较少为真实约束，不按商业团队规模设计。
3. 当“通用性、性能、开发速度、可解释性”冲突时，MVP 优先级为：可完成性 > 稳定性 > 可解释性 > 性能优化 > 通用性。
4. 对每项新技术都回答四个问题：它解决什么问题、为什么现在需要、最小使用方式是什么、不使用会怎样。
5. 不得用“视情况而定”结束关键决策。信息不足时可以明确写出合理假设，并在该假设下给出唯一建议。
6. 不要建议为了展示技术而引入无必要的中间件。未来扩展只能通过清晰接口预留，不提前实现。
7. 所有里程碑和验收标准必须可以实际观察或测量，避免“完善功能”“优化体验”这类无法验收的表述。
8. 如果某项需求在当前时间或能力约束下不可行，请直接指出，并给出能保留项目价值的降级方案。
9. 对安全和隐私保持保守：不进入游戏进程、不保存默认截图、不在客户端代码中硬编码未来的云端 API 密钥。
10. 最终回答应该让我能够据此开始开发，也能够在项目答辩时解释为什么做出这些架构选择。
```

## 推荐的后续提问顺序

完成第一轮立项分析后，可按以下顺序继续让 AI 协助，避免一次生成过多代码：

1. “基于刚才确定的路线，只细化第一周 P0 任务，给出每天的产出与验收方式，不生成后续周代码。”
2. “基于已确定的接口，先搭建 Electron、Vue 3 和 Python FastAPI 的最小通信骨架，并为每个文件解释职责。”
3. “在通信骨架通过验收后，实现一条最小链路：测试图片 → OCR → 标准化事件 → 固定模板弹幕 → 覆盖层。”
4. “核心链路稳定后，再设计 SQLite 表结构、迁移和历史记录，不要提前加入云端模型。”
